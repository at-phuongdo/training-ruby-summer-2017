<!DOCTYPE html>
<html>
<head>
  <title></title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <button class="btn"><%= link_to 'AddUser', new_user_path, :class => 'link-to' %></button>
    <button class="btn"><%= link_to 'Log out', logout_path, method: :delete,  :class => 'link-to' %></button>
    <div class="header">
      <h1>Books</h1>
    </div>
    <table class="index">
      <thead>
        <tr>
          <th>Image</th>
          <th>Name</th>
          <th>Author</th>
          <th>Title</th>
          <th>Isn</th>
          <th colspan="3">Action</th>
        </tr>
      </thead>

      <tbody>
      <% @book.each do |book|%>
        <tr>
          <td><%= image_tag book.image.thumb.url if book.image? %></td>
          <td><%= book.name %></td>
          <td><%= book.author %></td>
          <td><%= book.title %></td>
          <td><%= book.isn %></td>
          <td><%= link_to 'Show', book_path(book.id), :class => 'link-to' %></td>
          <td><%= link_to 'EditUser', edit_book_path(book), :class => 'link-to' %></td>
          <td><%= link_to 'Delete', book_path(book.id),
            method: :delete,
            data: { confirm: 'Are you sure?' } ,:class => 'link-to'%></td>
          </tr>
          <%end%>
        </tbody>
      </table>
    </div>
  </body>
  </html>